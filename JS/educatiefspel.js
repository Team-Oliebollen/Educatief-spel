function setup() {
  createCanvas(300, 200);
  textSize(18);
  
  image
  text("Answer:", 20, 20);
  usernameInput = createInput('Your answer', 'text');
  usernameInput.position(30, 40);
}
`// niet definitief.
